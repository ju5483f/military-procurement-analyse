# 國軍軍購案分析系統

離線分析工具，用於篩選國防部對美軍購案（FMS）之風險案件。

## 專案背景

- 每半年收到國防部主計局檢送的「執行中軍購案個案管制表」xlsx，需人工以 Excel 逐一套用分析條件（控制點）篩選風險案件。
- 本工具取代該人工流程。**目標執行環境：機密區的實體隔離電腦（無網際網路），以 Microsoft Edge 開啟。**
- 因此工具必須是**單一自足的 HTML 檔**，不得有任何外部依賴。

> 本資料夾內所有檔案均**不具機密性質**。真實機敏資料另存於實體隔離電腦，不會進入本專案。
> `國軍軍購案執行中個案管制表(範例).xlsx` 是依原始資料欄位與型態**自行捏造的假資料範本**。

## 檔案

| 檔案 | 說明 |
|---|---|
| `國軍軍購案分析系統v1150715.html` | **主要交付物**。20 項控制點（軍投 10＋作維 10）＋第二階段核對＋總表合計欄，單一自足 HTML（約 909KB），內嵌 SheetJS 0.18.5 |
| `錨點校準工具v1150717.html` | **獨立工具**（約 954KB）。為「風險分數」決定錨點用；帶進機密區上傳真實管制表，**系統自動算出 6 個參數的建議值**供參考（不自動套用），使用者決定後只帶回那 6 個數字。詳見下方「風險評分（設計中）」 |
| `國軍軍購案分析系統v1150710v2.html` | 17 項控制點＋第二階段會議紀錄核對，歷史版本**保留不動** |
| `國軍軍購案分析系統v1150710.html` | 17 項控制點版（無第二階段），歷史版本**保留不動** |
| `國軍軍購案分析系統.html` | v1 歷史版本（僅 2 個實驗條件），**保留不動** |
| `國軍軍購案執行中個案管制表(範例).xlsx` | 假資料範本，供開發測試（已含 P 欄待平衡支付值） |
| `會議資料(範例).xlsx` | 第二階段輸入的假資料範本（檢討態樣/案號/案名/說明或辦理情形 4 欄） |
| `分析結果表(範例).xlsx` | 分析總表版式範本（種類/編號/名稱/陸海空/其他/合計/第二階段） |
| `執行中軍購案個案管制表(演示用).xlsx` | **公開區演示用虛構管制表**（451 筆）。金額全為虛構，但案號沿用 `會議資料/` 之真實公開案號（陸21/海9 筆），故搭配該資料夾的 114-4 整理檔可完整演示第二階段。以 v1150715 分析之結果**與 `分析結果0716.xlsx` 完全相同**（120 格逐格驗證）——但 0716 本身有 3 處謄寫錯誤（見下列），故演示檔在那 3 格與真實資料不同。**不影響演示用途，不修**。產生腳本見 `演示資料產生腳本/` |
| `分析結果0716.xlsx` | 機密區實測 v1150715 後**手動整理**的結果概述（僅統計數字，不含個案資訊）。**不是直接匯出檔**——第二階段欄已由 `有出現（N件）` 簡化為數字、其他欄表頭已由 `其他（含中央或多軍種統建）` 縮短為 `其他`。**已知 3 處謄寫錯誤，全在空軍欄：2-1 應為 2（誤植 0）、2-3 應為 5（誤植 2）、2-5 應為 2（誤植 5）**；正確合計為 2-1=10、2-3=16、2-5=4。**此檔不可當作真值基準**（1150717 曾因此誤判校準工具有 bug，實則是基準有錯） |
| `控制點清單.docx` | 20 項分析控制點的需求來源 |
| `控制點篩選結果為所有軍購案評分的標準.docx` | **風險評分規則（設計中，尚未定案）**。9 項標「待定」。詳見下方「風險評分（設計中）」 |
| `程式需求與規劃.docx` | 最初的需求與規劃文件 |
| `國軍軍購案分析系統使用說明.md` / `.html` / `.docx` | **給承辦人員的操作手冊**（v1150715，A4 四頁列印版）。`.md` 為維護主檔，`.html` 為列印版，`.docx` 可編輯。修改控制點或流程時**務必同步更新** |
| `國軍對美軍購案控制點分析項目說明.docx` / `.pdf` | 控制點意涵與設計邏輯說明（**推測性質**，待向原設計者驗證） |
| `軍購管制會會議資料整理技能/` | 用不同 AI 工具（ChatGPT／Gemini／Claude）將軍購管制會會議資料整理成「第二階段」所需 4 欄 xlsx 的指令集，供在無法用本 Claude 帳號的環境下替代使用 |
| `演示資料產生腳本/` | 產生上述演示用管制表的 Node 腳本＋說明（含控制點的數學推導與求解方法）。**要重出演示檔就用這些腳本，不要從零重來** |
| `錨點校準腳本/` | `錨點校準工具v1150717.html` 的**維護主檔**（template，未內嵌 SheetJS，可直接編輯）＋獨立驗證腳本。**要改校準工具就改這裡的 template 再重新內嵌，不要改根目錄那個 939KB 的檔** |
| `會議資料/` | 各單位軍購管制會**實際**會議資料（陸軍／海軍 114-4 PDF）與其整理後的 4 欄 xlsx，供第二階段測試 |
| `參考資料/` | 審計會議簡報、在途物資管制、軍購執行實務等背景文獻 |

## 輸入資料欄位對應

xlsx 欄 A~O，程式以 **0-based 索引**存取。使用者需**手動刪除文件標題列**，使第一列即為欄位名稱。

| 索引 | 欄 | 欄位 |
|---|---|---|
| 0~6 | A~G | 項次、案號、軍種、單位、案名、**類別**、預算科目 |
| 7 | H | 發價值(A) 美元 |
| 8 | I | 累積結匯值(B) 美元 |
| 9 | J | 累計結匯率 (C)=(B)/(A) |
| 10 | K | 累積交運值(D) 美元 |
| 11 | L | 累計交運率 (E)=(D)/(B) |
| 12 | M | 軍購備查帳 (F)=(B)-(D) 美元 — **已驗證 M = I − K** |
| 13 | N | 軍購備查帳 (F)=(B)-(D) 新臺幣元 |
| 14 | O | FRB餘額 |
| 15 | P | 待平衡支付值 美元（> 0 即欠款案件） |

**類別欄（F）**值有 `軍投` / `作維` / `特別預算`。**特別預算併入「軍投」類**套用軍投控制點。

SheetJS 讀取公式欄（J、L）時取 XML `<v>` 的快取值，會直接得到數字，不需重算。

## 控制點清單（共 20 項，來源 `控制點清單.docx`；v1150715 起）

金額門檻單位皆為**美元**。「差異」一律取**絕對值** `|K/H − J|`。所有比率運算需防範除以零。

### 軍事投資建案（軍投，含特別預算）— 10 項

1. 交運值為 0 → `K == 0`
2. 累計交運率未達 50%（**不含 0**）→ `0 < L < 0.5`
3. `|K/H − J| ≥ 0.4`
4. `|K/H − J| < 0.4` 且 `M ≥ 3000萬`
5. `K/(I−O) < 0.1` 且 `|K − (I−O)| > 1億`
6. 結匯率超過 100% → `J > 1`（**v1150715 新增**）
7. `O/I ≥ 0.4`（舊 1-6 順移）
8. `O/I < 0.4` 且 `O ≥ 3000萬`（舊 1-7 順移）
9. 待平衡支付值 `> 0`（欠款案件）（舊 1-8 順移）
10. `J ≥ 0.9` 且 `L < 0.5`（累計結匯率達90%且交運率未達50%，**v1150715 新增**）

### 作業維持案（作維）— 10 項

1. `K == 0` 且 `I ≥ 300萬`
2. `0 < L < 0.5`（不含 0）且 `M ≥ 3000萬`
3. `|K/H − J| ≥ 0.5` 且 `M ≥ 300萬`
4. `|K/H − J| < 0.5` 且 `M ≥ 3000萬`
5. `K/(I−O) < 0.1` 且 `|K − (I−O)| > 1000萬`
6. 結匯率超過 100% → `J > 1`
7. `O/I ≥ 0.5` 且 `O ≥ 300萬`
8. `O/I < 0.5` 且 `O ≥ 1000萬`
9. 待平衡支付值 `> 0`（欠款案件）
10. `J ≥ 0.9` 且 `L < 0.5`（**v1150715 新增**）

**編號異動提醒**：v1150715 於軍投插入新 1-6，舊 1-6/1-7/1-8 順移為 1-7/1-8/1-9；作維 2-1~2-9 不變。1-10/2-10 為新增複合條件（軍投作維參數獨立）。1-2 由「未達50%」修正為「不含0」。

原文誤字：軍投項 4「軍購**被**查帳」應為「軍購**備**查帳」；作維項 9「之**軍事投資**案件」應為作維案件。

## 控制點的設計邏輯

> **重要：讀過再改門檻。以下差異都是刻意的，不是筆誤。**

FMS 採先付款後交貨：我方依發價書（LOA）與美軍季帳單（645 表）**先結匯付款** → 款項存入美國聯邦儲備銀行紐約分行的計息帳戶（**FRB**）→ 美方 DFAS 每月按未來約 30 日實需自 FRB 提領至信託基金支付合約商 → 美方交貨後於季帳單列計**交運值** → 我方驗收並沖銷在途物資。

**軍購備查帳餘額 M = 結匯 − 交運 = 在途物資**，即「已付款、未交貨、滯存美方」的金額，是審計檢討的核心焦點。

控制點沿著「錢的旅程」設計，各對應一個風險節點（節點編號以軍投為例，v1150715 後）：

- **結匯滯留**（軍投 1-7/1-8）— 錢匯出後大量閒置於 FRB，付款超前美方實需
- **提領未交**（軍投 1-5）— `I − O` ≒ 美方已自 FRB 提領動支的金額；交運值不及其一成 → 履約異常，風險最高
- **交貨落後**（軍投 1-2/1-3/1-4、1-10）— 交運進度落後付款進度，在途物資偏高
- **例外態樣** — 結匯率 > 100%（軍投 1-6，溢付帳務異常）、待平衡支付值 > 0（軍投 1-9，我方欠款）

兩個關鍵慣例：

1. **比率 ＋ 金額雙保險**：成對出現的控制點（如軍投 1-3/1-4、1-7/1-8）**不是重複**。比率負責抓異常型態，絕對金額門檻負責抓滯美資金重大的大案，互補防漏。
2. **軍投 40% vs 作維 50% 的門檻差異是刻意的**。作維類（開放式軍購、CLSSA 一號／二號訂單）結匯先行、分批交運、案內常無實際交運品項，落差屬常態，故門檻放寬並多加金額門檻以排除小額案噪音。**不要「順手改成一致」。**
3. **1-10/2-10（結匯率≥90% 且交運率<50%）是審計實務常用的複合篩選**，與 1-2（交運率<50%）、1-6（結匯率>100%）部分重疊屬正常，各自對應不同檢討態樣，不要合併。

## 程式行為（v1150715）

- 四步驟流程：① 上傳執行中管制表 → ② 預覽完整資料｜確認欄位 → ③ 分析條件設定（24 個門檻參數全部可調，軍投／作維摺疊面板）→ ④ 分析總表
- **欄位錯位防呆（步驟②）**：程式依「欄位位置」而非名稱讀取資料，故國防部若調換欄位順序會造成靜默的錯誤分析。預覽區因此設計為：表頭區顯示 `Excel 欄號` ＋ `系統預設欄位`，表身第一列（黃底）顯示**上傳檔案的原始表頭**供逐欄核對。`headerMismatches()` 以 `normHeader()`（去除所有空白與換行）比對兩者，不符欄位標紅並跳出紅色警示；`runAnalysis()` 於有不符時以 `confirm()` 二次確認。檔案欄數多於 16 時亦提示忽略。
- **合計列防呆（v1150715）**：步驟①警語提醒手動刪除「合計」列；`detectTotalRow()` 檢查最末筆 A~G 任一儲存格含「合計/總計」→ 預覽區紅色警示＋`runAnalysis()` confirm 二次確認（不自動刪除）。
- **單一資料結構 `CONTROL_POINTS` 驅動一切**（20 項定義：id、cat、name、inputs、test）——新增／修改控制點只改這個陣列，閾值面板、分析、總表、詳細頁、匯出全部自動跟上
- 成對控制點共用同一門檻輸入框（1-3/1-4、1-7/1-8、2-3/2-4、2-7/2-8），維持互斥分割
- 依「類別」欄分流：`作維` 套作維 10 項；`軍投`＋`特別預算` 套軍投 10 項
- 軍種計數歸類：恰為 `陸軍`/`海軍`/`空軍`（trim 後）各自計數，其餘（中央、多軍種統建）歸「其他」
- **符合多個控制點的案件，各列一筆**（不合併）
- SPA 視圖切換：主頁面（分析總表）⇄ 20 個詳細頁面；狀態存記憶體，返回不需重新分析；0 案的明細表按鈕 disabled
- **分析總表欄位**：畫面＝編號｜名稱｜陸｜海｜空｜其他｜**合計**｜第二階段｜明細表按鈕（合計＝四軍種和＝該控制點結果筆數）
- 輸出統一 18 欄＝分析項目名稱＋原始 16 欄＋計算欄「累計交運值/發價值(D/A)」（插在交運值後）；「分析項目名稱」格式如 `軍投1-3 「累計交運值/發價值」…`
- 三種 xlsx 匯出（檔名日期=今日 YYYYMMDD）：
  - `{date}軍購案分析總表.xlsx`（**9 欄**：種類｜編號｜名稱｜陸｜海｜空｜其他｜合計｜第二階段 × 20 列；**匯出檔含「種類」欄，畫面不含**）
  - `{date}全部分析結果明細表.xlsx`（單一工作表堆疊）
  - 詳細頁 `{date}{類別}{編號}{名稱}.xlsx`，**第 1 列為標題「控制點{編號}：{名稱}」**（列印辨識用），第 2 列起為表頭＋資料
- 檔名消毒：`\/:*?"<>|` → 全形（1-3 名稱含 `/`）
- 匯出建構（`buildSummaryAOA`/`buildAllAOA`/`buildDetailAOA`）與下載（`downloadAOA`）分離，便於自動化驗證

### 第二階段：會議紀錄核對（v1150710v2 新增，選用）

- 第一階段分析完成後顯示「⑤ 第二階段」區塊：上傳會議資料整理檔（可多檔、可逐一移除），格式＝第一列表頭、A~D 欄「檢討態樣/案號/案名/說明或辦理情形」，僅讀第一個工作表；欄名不符比照第一階段警示＋confirm
- 核對規則：第一階段結果的「案號」`normCaseNo()`（去空白＋大寫）精確比對；命中者於結果列末補 2 欄（索引 18=檢討態樣、19=說明或辦理情形）
- 多筆合併：同案號多筆（跨檔案亦然）於單一儲存格「1. …\n2. …」編號合併，態樣與說明同步編號；單筆不編號；（案號,態樣,說明）完全相同者去重
- 總表新欄「第二階段：核對會議紀錄」（其他與詳細情形之間，畫面恆顯示）：未核對空白；核對後有 N 件命中＝`有出現（N件）`
- 動態表頭 `outHeaders()`：未核對 18 欄、核對後 20 欄——詳細頁與三種下載自動跟隨；總表匯出恆 7 欄（含第二階段欄）
- 重新執行第一階段分析會**自動重新核對**（會議檔留在記憶體）；移除全部會議檔＝重置（列裁回 18 欄、總表欄清空）
- 既知限制：xlsx 儲存格內 `\n` 需在 Excel 開「自動換行」才顯示多行（SheetJS 社群版無法寫入樣式）

**第二階段驗收基準**：範例會議檔（63 筆、海軍 TWP 案號）與第一階段命中案號無交集→核對後全空白。測試檔（scratchpad `make_mtg_test.js`：TWBZAB 兩態樣、TWBZAT 重複列、TWDSKC、TWZZZZ 無關案號）＋範例檔兩檔核對：1-2＝2件、1-3＝2件、1-4＝1件（244 特別預算）、1-7＝2件、其餘空白，命中列共 7 筆；獵狐編號合併 2 筆、瑞杰去重後單筆不編號。

## 風險評分（設計中）

目標：以分析結果對當期全部軍購案評 0~10 分／控制點，加總為風險總分。規則寫在 `控制點篩選結果為所有軍購案評分的標準.docx`，其中 9 項標「待定」。**評分功能尚未做進主系統**，v1150715 不受影響。

### 錨點：已於 1150717 用真實資料校準定案

使用者帶 `錨點校準工具v1150717.html` 進機密區跑真實管制表，帶回這 6 個數字：

| 參數 | 定案值 | 來源 |
|---|---|---|
| 金額上錨 | **100 億美元** | 工具建議（50 億是貼頂歸零的最小候選，×2） |
| 金額下錨 | **1 萬美元** | 工具建議（自訂統一值） |
| 待平衡支付值下錨 | **1 萬美元** | 工具建議 |
| 待平衡支付值上錨 | **2 億美元** | 工具建議（1 億 ×2） |
| 結匯率上錨 | **300％** | **使用者判斷，推翻工具的「無法建議」**（見下） |
| 雙軸權重 w | **0.50** | 維持預設 |

**驗收：C1 全數通過**——無任何維度出現「偏窄，錨點可能太寬」；雙軸維度最高 7.84~8.7（天花板本來就低，符合設計預期）、單軸維度摸到 10。金額上錨 100 億反推可知本期最大備查帳／FRB 餘額落在 20~50 億美元之間，與台灣對美軍購的規模相符。

### 真實資料的兩個發現（1150717）

**1. 有一案結匯率 1950％。** 工具的候選上錨最高 200％，故顯示「所有候選皆貼頂、無法建議」。使用者查了實際資料：最高 4 名中，第 1 名 1950％，其餘 3 名皆在 300％以內 → 自行訂 300％。**此判斷正確**：若照規則硬設 1950％，250％ 的案子只會拿到 `(2.5−1)/(19.5−1)=0.08` 的嚴重度，整條軸就廢了。真正決定 300％ 的是第 2~4 名，不是那個離群值。
> **1950％ 值得單獨追查**（尚未查證）。結匯率＝結匯值÷發價值，19.5 倍通常代表**發價值欄位有問題**（LOA 大幅修訂未更新、或填了佔位數字）。因為 `diff = J(1−L)`，J=19.5 會讓 1-3/2-3 的差異軸直接貼頂；交運率 K/I 也會被壓低。**一個發價值錯誤的案子會在多個控制點同時亮紅燈，看起來風險極高，但其實是帳錯了**——檢討方向是查帳，不是催交貨。

**2. 有一案待平衡支付值＝74 美元**，低於下錨 1 萬 → 該軸貼底、得 1.00 分。工具跳黃色警示，使用者查證後接受。**這是正確結果，不是問題**：欠款 74 美元的案件命中「欠款案件」控制點、拿最低分，本來就該如此。貼底警示的語意是「請確認這是不是你要的」，不是「這裡有錯」。

> **重要：以下三件事是推導出來的，不是偏好。改評分設計前先讀完。**

**1. 成對控制點必須合併為單一評分維度，共用雙軸公式。**
關鍵代數：**`diff = |K/H − J| = J(1−L) = M/H`**。所以 1-3 的「差異」與 1-4 的「備查帳餘額」**是同一筆錢 M 的兩種看法**（除以發價值 vs 不除）；1-7/1-8 是 O/I 與 O，同構。若各自單軸計分，會出現「兩軸皆較差、分數反而較低」的反轉——演示檔實例：TWBZBY（diff=0.51、M=2.64億）得 4.46 分，TWDUMP（diff=0.22、M=2.56億）得 9.40 分。故 1-3/1-4、1-7/1-8、2-3/2-4、2-7/2-8 四對各合併為一個維度，共 **16 個評分維度**。

**2. 採數值錨點法，不採排序法。** 理由是跨期可比；且排序法在 N=1 時失真（演示檔 1-5、1-9 皆僅 1 件命中）。兩法在**總分**層級高度一致（Spearman 0.92），但在**單一維度**層級差異大（上錨 1億 vs 10億 → 相關 0.74），而明細表要顯示個別控制點分數。

**3. 已知結構性偏誤（未修）**：雙軸維度要兩軸同時滿分才得 10，實務上不會發生（演示檔 1-3/1-4 最高 6.90、1-7/1-8 最高 6.92），而單軸維度可達 10（2-9、2-10）。加總時單軸維度的實質權重因此偏高。

其他未定案項目：總分尺規（單案最多命中 7 個維度＝上限 70，要不要拉回 0~100）、1-10 必然同時命中 1-3（代數：J≥0.9 且 L<0.5 → diff>0.45>0.4；演示檔 3/3 驗證）造成的重複計分是否接受、2-1 是否改為隨結匯值計分。

### 錨點校準工具 v1150717

錨點要看真實金額分布才訂得準，而該資料只在機密區 → 做成獨立 HTML 帶進去現場互動試算，**只帶回 6 個參數數字**（使用者的政策判斷，非資料）。

- 6 個參數：金額上錨（預設 10 億）、金額下錨（預設＝各控制點門檻，可切自訂）、待平衡支付值上/下錨（1000 萬／10 萬）、結匯率上錨（120％）、雙軸權重 w（0.5）
- **下錨比上錨更關鍵**：演示檔 1-7/1-8 群體 32 案有 17 案的 O 低於門檻 3000 萬 → 金額軸恆為 0、分數上限僅 6.92；下錨改 10 萬後貼底 0 案、分數變 3.85~8.58
- 校準目標是「各維度分數合理鋪開於 1~10」（看 ④-C1），不只是「避免截斷」

**自動建議（`recommend()`）** — 規則即：**下錨取「貼底歸零的最大候選」，上錨取「貼頂歸零的最小候選」×2**（結匯率不 ×2，比率有實務上界）。

> **三個不可誤改的性質**
> 1. **建議值只從候選階梯取，不由資料直接算**。階梯值僅透露「最大值落在哪一級距」（與 B-2／B-3 既有洩漏程度相同），而 `上錨 = 最大值 × 2 = 21.88 億` 等於直接公布金額。
> 2. **建議值不得依賴目前參數**，否則使用者一調整、建議就跟著漂移而失去參考價值。`pinCount()` 的 `over` 參數即為此設計。無資料可依據時（如 1-6／2-6 皆未命中）**留白，不可回填 `A.jHi`**。
> 3. **載入時不自動套用**（使用者要求自己決定）。打開時 C1 就是難看的 1.28~6.90，那個難看本身是資訊。

> **陷阱**：`A.jHi` 的單位是**百分數**（120），`effJHi()` 才除以 100。覆寫時（`pinCount(..., {jHi: x}, ...)`）必須傳百分數；傳 1.2 會變成結匯率 1.2%。`renderB4()` 與 `recommend()` 皆已處理。

**下錨是單一共用值，必然是妥協（畫面綠色面板有明文列出各軸理想值，不可簡化）**：各金額軸理想下錨相差 300 倍——1-3/1-4→10萬、1-7/1-8→10萬、2-3/2-4→300萬、2-7/2-8→300萬、2-5→1000萬、1-5→3000萬、2-2→3000萬。**成因是結構性的：1-3 與 1-7 是配對中唯二「沒有金額條件」的控制點**（1-3 只看差異、1-7 只看占比），其群體因此混入金額極小的案件；其餘五軸的最小值都落在自己門檻上方，門檻當下錨本來就對。共用值取最小者（10 萬）保證無貼底，代價是其他軸略被壓縮——但實測後各維度反而更一致（3.5~8.8），對加總未必是壞事。

**兩個判讀陷阱（已寫進畫面③的操作要領）**：
- **比率軸的貼頂不用管**——那代表案件真的落在天然極端（差異 100%、交運/已提領 0%），本來就該滿分。只有金額軸的貼頂要調。演示檔套用建議後仍殘留 3 個比率軸貼頂（2-3/2-4 差異、2-5 交運/已提領、2-10 差距），屬正常。
- **雙軸維度天花板本來就低**，不必勉強調到 10。
- 沿用 v1150715 的上傳／預覽／欄位錯位防呆／合計列防呆；**移除**閾值面板、詳細頁、SPA、第二階段；24 個控制點門檻固定用預設值
- **機密性分級（畫面⑤有明文，不可簡化成「不含金額」）**：A、C 區純計數與分數；**B 區雖只有案件數，但候選錨點對照會以級距反映金額範圍**（「10億貼頂1案、20億貼頂0案」＝最大值在 10~20 億之間）；D 區含真實金額，預設摺疊且匯出預設不勾。**工具不對可攜出性做判定**，該判定屬使用者權責

**驗收基準（演示檔 451 筆，`錨點校準腳本/expected_calib.js` 獨立實作）**：A 表 1-1=5、1-2=26、1-3/1-4=30、1-5=1、1-6=0、1-7/1-8=32、1-9=1、1-10=3、2-1=8、2-2=4、2-3/2-4=19、2-5=7、2-6=27、2-7/2-8=54、2-9=24、2-10=21；有分數 172 案／未命中 279 案；預設參數下總分最高 32.64；貼底 1-3/1-4=6、1-7/1-8=17；跨軸去重貼頂 vs 金額上錨＝1億→33、3億→9、10億→1、20億以上→0。

**建議值驗收（演示檔）**：金額上錨 **40 億**（20 億 ×2）、金額下錨 **自訂 10 萬**、待平衡支付值下錨 **10 萬**、上錨 **1 億**（5000 萬 ×2）、結匯率上錨 **120%**、w **0.50**。套用後 C1：1-3/1-4→3.68~8.03、1-7/1-8→3.75~8.12，金額軸貼頂貼底全歸零。

> **陷阱**：「跨軸去重貼頂案件數」**只能計受金額上錨管轄的軸**。待平衡支付值軸受 P 上錨管轄，計入會造成恆定 +1 偏移（早期腳本犯過此錯，誤得「10億→2案」）。徵兆是上錨拉到 500 億時貼頂數仍不歸零。

### 驗收基準（範例 xlsx，15 筆）

欄位錯位防呆已以「H↔I 對調」的測試檔驗證：正確標出 2 個不符欄、跳出 confirm、取消後不執行分析。
合計列防呆已以「末列 E 欄=合計」測試檔驗證：紅色警示＋confirm、取消後不執行分析。

20×4 計數矩陣已以獨立 Node 腳本雙重驗證（scratchpad `expected3.js`）。非零項（v1150715）：
1-2＝5（項次 9,10,156,232,446）、1-3＝3（9,10,446）、1-4＝2（232,244 特別預算）、1-5＝1（232）、1-7＝2（156,446）、1-8＝3（9,10,232）、1-10＝1（10 瑞杰）；1-6（J>1）與全部作維皆 0 案；1-2 排除 L=0 未改變結果（命中案件皆 L>0）。項次 446 軍種「中央、陸軍」歸「其他」。

## 開發注意事項

- **機密區帶回的「結果概述」是手抄的，不是匯出檔——不可當真值基準。** `分析結果0716.xlsx` 就有 3 處謄寫錯誤（見檔案表）。判別方法：對照工具的實際匯出格式（如第二階段欄應為 `有出現（N件）` 而非純數字、其他欄表頭應為完整的 `其他（含中央或多軍種統建）`）。**若手抄檔與工具算出的結果不一致，先懷疑手抄檔**——1150717 曾反向推論，誤判校準工具有 bug，浪費一輪往返。使用者提供的畫面截圖（工具實際輸出）優先於任何整理過的檔案。
- **SheetJS 必須內嵌**，不可用 CDN。做法：在 HTML 模板放 `<!--SHEETJS_PLACEHOLDER-->`，用 PowerShell `.Replace()` 將 `xlsx.full.min.js` 內容直接在磁碟上替換進去，避免把 882KB 的 JS 讀進編輯器。
- 範例 xlsx 常被 Excel 鎖定。讀取前先 `Copy-Item` 到暫存目錄再讀。
- 本機無 pandoc、LibreOffice、Python。讀 docx/xlsx/pptx 請用 PowerShell 的 `System.IO.Compression.ZipFile` 直接解 XML；讀 PDF 用 Git 附帶的 `pdftotext.exe`；產生 docx 用 `npm i -g docx`（Node），轉 PDF 用 Word COM 自動化。
- **本專案跨機器作業**（辦公室本機 ＋ claude.ai/code 雲端），遠端 `origin/main` 可能已有本機沒有的 commit。**開工前先 `git fetch` 再看 `git status`**——直接看 `git status` 只會比對本機的追蹤參照，可能是舊的（曾發生本機落後 6 個 commit 而不自知）。雲端工作會開 `claude/xxx` 分支發 PR，合併後記得刪分支。

## 待辦

1. 使用者於 Edge／機密區實機驗收 v1150715（20 項控制點＋合計欄＋合計列防呆＋第二階段）
2. 說明表為推測性質，建議以第 2305 次審計會議簡報第 13 頁（購案初期／中期／末期分類）向原設計者查證
3. **風險評分**：錨點已於 1150717 校準定案（見上）。**下一步＝依這 6 個數字改寫 `控制點篩選結果為所有軍購案評分的標準.docx`，補完 9 項「待定」**，再把評分做進主系統。動筆前須先決定三個結構問題：
   - **總分尺規**：單案最多命中 7 個維度＝上限 70，要接受 0~70 還是拉回 0~100？
   - **1-10 的重複計分**：代數上 1-10 必然同時命中 1-3（J≥0.9 且 L<0.5 → diff>0.45>0.4），L>0 時 1-2 也必中。同一個「結匯快、交運慢」現象被計三次分，是否接受？
   - **2-1 的計分方式**：目前「命中即 10 分」，但它有兩個條件（K=0 且 I≥300萬）。真實資料本期 2-1 有 10 案全部固定 10 分。要不要改為隨結匯值計分？（工具的⑤有試算區塊）
4. 追查結匯率 1950％ 那一案的發價值欄是否有誤（見上「真實資料的兩個發現」）
