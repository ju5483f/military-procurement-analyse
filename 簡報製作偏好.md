# 簡報製作偏好與規範（審計部 第二廳 · DBA 專案）

> **這份檔案的用途**：把使用者製作投影片的習慣、版型、色彩、字級、圖示與文案慣例固定下來。
> 之後在任何新對話中只要上傳本檔，即可直接依此規範產出或修改簡報，不必重新問一次。
>
> 依據來源：`DBA_簡報_15分鐘版_美編.pptx`（AI 初版）與使用者親手改版之 `簡報_15分鐘版.pptx` 的逐頁比對，
> 加上使用者既有作品 `【第二廳】行政法人審計事務-115年度創新共識營.pptx` 的共通慣例。

---

## 0. 給 AI 的操作指令（先讀這段）

1. **先讀完本檔再動手**。版型、字級、色碼都已定案，不要另外發明配色或版面。
2. **文字與圖片必須可編輯**：一律用原生文字方塊與可移動／可替換的圖片物件，**不得把整頁做成一張圖**。
3. **圖示先用本檔第 6 節的單色圖示庫做版**，使用者會自行替換成彩色扁平圖示（Flaticon 風）。
   因此圖示一律獨立擺放、不要與其他物件群組，方便整顆換掉。
4. **產生前先給樣稿**：新版型或大幅改版，先用 HTML 1280×720 渲染 PNG 給使用者看過再產 pptx。
   （使用者的固定要求是「先給我看樣稿，不要開始動手」。）
5. **不確定的地方要問**，不要自行猜測後直接產出整份檔案。
6. 產出後附上「哪幾頁需要使用者自己補圖／補截圖」的清單。

---

## 1. 基本規格

| 項目 | 規定 |
|---|---|
| 版面 | 16:9，**13.333 × 7.5 英吋**（pptxgenjs 用 `LAYOUT_WIDE`；等於 1280 × 720 px @96dpi） |
| 中文字型 | **微軟正黑體**（字串就寫「微軟正黑體」，不要寫 Microsoft JhengHei） |
| 數字／英文 | 同上；編號、代碼類文字可用 Consolas |
| 左右邊界 | **70 px（0.73 in）**，所有標題與內文一律對齊此線 |
| 內容區 | y = 150 ~ 660 px（標題以下、下邊界以上） |
| 頁尾 | **不放頁尾**（不放系統名稱） |
| 頁碼 | **整份簡報都不要放頁碼**——連封面封底以外的內頁也不要。使用者會自行於投影片母片統一設定 |
| 講者備忘稿 | **不需要**（除非另外要求） |

---

## 2. 色彩系統

| 用途 | 色碼 | 說明 |
|---|---|---|
| 主色／主要文字 | `#2A2D32` | 深炭。標題、內文、深底色塊、第一順位圖示底 |
| 次色 | `#435D74` | 深藍灰。章節編號徽章、圖示底、次要標題 |
| 第三色 | `#5F8195` | 淺藍灰。圖示底、輔助元素 |
| 第四色 | `#455A45` | 橄欖綠。僅用於圖示底輪替 |
| **唯一強調色** | `#E0A215` | 琥珀。小標記、重點色塊；**整份簡報只用這一個強調色** |
| 琥珀文字 | `#B07D0A` | 註解、引導字（琥珀在白底上的可讀版本） |
| 重點框底 | `#FFF2CC` | 淡黃。**重點說明／舉例／設計核心**區塊的底色 |
| 面板底 | `#EDEFEA` | 淺灰綠。一般說明面板 |
| 面板底（深一階） | `#DBDDD3` | 深底頁上的淺色文字亦用此色 |
| 次要內文 | `#5C6771` | 卡片說明文字 |
| 註解文字 | `#8A928B` | |
| 分隔線／箭頭 | `#C3CCD3` | |
| **關鍵詞紅** | `#C00000` | 內文中標關鍵詞用，**粗體**。（使用者原本習慣純紅 `#FF0000`，若要改回直接全域替換即可） |

**禁用**：Office 佈景主題預設色，特別是橘 `#ED7D31`、藍 `#0070C0`、標準藍 `#4472C4`。
需要強調時只用琥珀 `#E0A215` 或關鍵詞紅。

**色彩比重**：白底為主（60~70%），深炭與藍灰為結構色，琥珀只點綴（單頁不超過 2 處）。

---

## 3. 字級系統

**內文下限 16 pt，密頁也不得低於 14 pt。** 寧可拆頁，不要縮字。

| 用途 | pt | px（1280×720 樣稿用） |
|---|---|---|
| 封面主標題 | 40 | 53.3 |
| **章節分隔頁標題** | **60** | 80 |
| 章節編號（01~04） | 40 | 53.3 |
| **內容頁標題** | **28** | 37.3 |
| 大綱項目標題 | 24 | 32 |
| 區塊／卡片標題 | 20 | 26.7 |
| **段落內文** | **18** | 24 |
| 卡片／多欄內文 | 16 | 21.3 |
| 流程節點標籤 | 16~18 | 21.3~24 |
| 表格內文 | 14 | 18.7 |
| 註解／引導字 | 12 | 16 |

- 標題一律**粗體**。
- 內文**不要整段粗體**；只有「需要唸出來的重點段」整段加粗，其餘維持一般字重。
- 行距：標題 1.05，內文 1.1~1.15（樣稿 CSS 用 `line-height:1.4~1.55`）。

---

## 4. 標題與章節體系

1. **單層標題**。不要用「小標籤＋主標」兩層（早期版本的 12pt 琥珀小標籤已廢除）。
2. 對策／策略類頁面，標題格式固定為：
   **`策略N：主題敘述`**（阿拉伯數字，如「策略1：離線單機、單一自足，嚴守機敏規範」）
3. 其他章節頁標題格式：**`大類｜子題`**（如「未來精進作為｜短期規劃」）。
4. **不用「壹貳參肆」中文序號**；章節以 `01`~`04` 數字徽章表示。
5. 數字徽章：`#435D74` 圓角方框（88×86 px，圓角 14），白色 40 pt 粗體數字。
   可放在**分隔頁**，也可放在**該章第一張內容頁的左上角**（此時標題右移讓位）。

---

## 5. 版型庫

座標一律以 1280×720 px 描述（換算成英吋：`in = px / 96`）。

### 5.1 封面
- 深炭 `#2A2D32` 滿版底，右下角以 5~6 根不同高度的長條（藍灰／米白／琥珀）作裝飾。
- 左上琥珀小方塊 20×20 px。
- 主標 40 pt 米白 `#EDEFEA`，副標 24 pt `#DBDDD3`，英文全稱 12 pt `#8FA6B7`。
- 底部：`報告單位：審計部 第二廳　報告人：第一科 ○○○`（12.8 pt）＋日期 `115.07.29` 格式。

### 5.2 報告大綱
- 標題「**報告大綱**」32 pt（就用這四個字，不要「簡報大綱／本日報告架構」雙層）。
- 2×2 排列，每項＝`#435D74` 圓形編號（64 px，白色 18 pt 數字）＋ 24 pt 標題 ＋ 14 pt 灰色說明。

### 5.3 章節分隔頁 ★
- **只放分隔內容，不要疊其他頁面的殘留物件。**
- 構成：60 pt 粗體標題（置中偏左）＋ 數字徽章 ＋ **與該章主題相關的圖示**（必要，見第 6 節）
  ＋ 四角淡影裝飾（黑 `#000000`、透明度 90%，不規則塊，來自 Slidesgo 軍事模板；沒有素材時可省略）。

### 5.4 標準內容頁（左右分欄）
- 標題 28 pt @ (70, 48)。
- 左欄 70~610、右欄 650~1210；內容起始 y = 150。
- 右欄常用 `#EDEFEA` 面板（圓角 18），內含 2~4 列「圖示＋20 pt 標題＋16 pt 說明」。

### 5.5 卡片列（3~5 欄）
- 白底卡片、圓角 14、陰影 `0 6px 18px rgba(42,45,50,.09)`。
- 卡片內：圖示（圓底 A 型）→ 20 pt 標題 → 16 pt 說明；右上角可放灰色序號 `01`。
- 5 欄時卡片寬 210~230 px、間距 22 px。

### 5.6 流程節點頁 ★（最常用）
- 3~5 個節點水平排列，每節點＝**A 型圓底圖示（直徑 104 px）**＋ 下方 16~18 pt 粗體標籤。
- 節點間放 `▶`（24 px，`#C3CCD3`）。
- 下方接「**設計核心／說明**」區塊：**淡黃 `#FFF2CC` 圓角框**，內含一句大字結論（20~26 pt）＋ 2 行說明（16 pt）。
- 例：`結匯付款 ▶ 款存 FRB ▶ 美方提領 ▶ 交貨列計交運 ▶ 驗收沖銷`。

### 5.7 表格頁 ★
- 清單型內容（控制點、指標、法條）**一律用真表格**，不要改寫成散落的短句。
- 欄位設計必含一欄**白話「風險意涵／目的」**，把技術定義翻成一句人話。
- 樣式：表頭 `#435D74` 底白字粗體、隔行 `#F4F6F5`、格線 `#DBDDD3`、內文 14 pt、表頭置中、內文靠左。
- 一頁最多 10~11 列；超過就拆頁（軍投一頁、作維一頁）。

### 5.8 系統截圖頁 ★
- 截圖**滿版或近滿版**呈現（可做到 13.33 × 6.4 in）。
- **不加標題**，只加**一句說明框**（淡黃 `#FFF2CC` 或白底，16~18 pt），必要時加細引線指向截圖重點。
- 截圖須由使用者提供；產出時先放同尺寸灰色預留框並在交付說明中列出。

### 5.9 階梯卡片頁（短／中／長期）
- 三張卡片等寬（368 px），**由左至右逐階升高**（top 依序 433 / 360 / 287 px），下緣**刻意出血**至版面外。
- 卡片內只放圖示 ＋ 32 pt 標籤（短期／中期／長期）＋ 一句 16 pt 說明；細節放到後續頁。

### 5.10 結語
- 深炭滿版底，頂端一條琥珀細線（高 3~4 px）。
- 「結語」18 pt `#DBDDD3` 當標記，兩段 20 pt 白字。

### 5.11 封底
- 深炭滿版底，「**敬請指教**」48 pt 白字置中，下方系統名 13.5 pt、`Q & A　｜　報告單位：審計部 第二廳` 10.5 pt。

---

## 6. 圖示規範

### 6.1 兩種處理方式與使用時機（**混用**）

| 型式 | 外觀 | 用在哪裡 | 尺寸 |
|---|---|---|---|
| **A 圓底單色** | 主色實心圓 ＋ 白色圖示 | **流程節點、卡片頭、需要醒目的地方** | 圓 104 px（節點）／60~64 px（卡片、列表頭）／44 px（小列） |
| **B 裸線單色** | 無底，統一 `#435D74` 線條 | **說明列表、密集列、不搶字的地方** | 56~92 px |

- A 型：圖示佔圓直徑 **46%**，白色描邊 `stroke-width:1.9`。
- B 型：描邊 `stroke-width:1.7`，色 `#435D74`。
- A 型圓底色**依序輪替**：`#2A2D32` → `#435D74` → `#5F8195` → `#455A45`。**琥珀不當圖示底**（保留給強調）。
- 圖示一律**獨立物件**，不與文字群組，方便使用者整顆替換成彩色圖示。
- 使用者會自行替換為**彩色扁平圖示**（Flaticon flat 風：粗描邊＋色塊）。同一顆圖示在不同頁重複使用是常態，不必每頁換新。

### 6.2 內建單色圖示庫（SVG path，viewBox `0 0 24 24`）

線條型（描邊，不填色）：

```
chip        <rect x="4" y="4" width="16" height="16" rx="2"/><path d="M9 9h6v6H9z"/><path d="M2 9h2M2 15h2M20 9h2M20 15h2M9 2v2M15 2v2M9 20v2M15 20v2"/>
memcpu      <path d="M4 7h16v10H4z"/><path d="M8 11h8M8 14h5"/><path d="M12 3v4M7 20v1M12 20v1M17 20v1"/>
doc         <rect x="5" y="3" width="14" height="18" rx="2"/><path d="M8 8h8M8 12h8M8 16h5"/>
wifioff     <path d="M2 8.5C5 6 8.5 4.7 12 4.7c1.6 0 3.1.3 4.6.8"/><path d="M5.5 12c1.5-1.1 3.2-1.8 5-2M18.5 11.4c.7.3 1.4.8 2 1.3"/><path d="M9 15.5c.9-.6 1.9-1 3-1s2.1.4 3 1"/><path d="M3 3l18 18"/>  ＋ dot(12,19,r1)
shieldcheck <path d="M12 3l7 3v5c0 4.5-3 7.5-7 9-4-1.5-7-4.5-7-9V6z"/><path d="M9 12l2 2 4-4"/>
lock        <rect x="5" y="11" width="14" height="9" rx="2"/><path d="M8 11V8a4 4 0 018 0v3"/>
payment     <rect x="2" y="7" width="20" height="10" rx="2"/><circle cx="12" cy="12" r="2.6"/><path d="M6 12h.01M18 12h.01"/>
bank        <path d="M3 9l9-5 9 5"/><path d="M5 9v9M19 9v9M4 19h16"/><path d="M9 12v4M12 12v4M15 12v4"/>
withdraw    <path d="M4 20h16"/><rect x="5" y="12" width="4" height="6"/><rect x="15" y="9" width="4" height="9"/><path d="M12 3v8M9 8l3 3 3-3"/>
box         <path d="M3 8l9-4 9 4-9 4z"/><path d="M3 8v8l9 4 9-4V8"/><path d="M12 12v8"/>
tank        <rect x="2.5" y="15.5" width="19" height="4.6" rx="2.3"/><path d="M4.2 15.5l1.4-3.6h10.8l1.4 3.6"/><rect x="8.5" y="7.4" width="6.2" height="4.5" rx="1"/><path d="M14.7 9.5h7.3"/><circle cx="6.6" cy="17.8" r=".9"/><circle cx="10.5" cy="17.8" r=".9"/><circle cx="14.4" cy="17.8" r=".9"/>
funnel      <path d="M3 5h18l-7 8v6l-4-2v-4z"/>
balance     <path d="M12 3v18M7 21h10"/><path d="M6 8l6-2 6 2"/><path d="M3 12a3 3 0 006 0M15 12a3 3 0 006 0"/>
gauge       <path d="M4 20a8 8 0 0116 0"/><path d="M12 20l5-6"/>  ＋ dot(12,20,r1.5)
scatter     <path d="M4 4v16h16"/><circle cx="9" cy="14" r="1.7"/><circle cx="13" cy="9" r="2.3"/><circle cx="17.5" cy="12.5" r="1.4"/>
search      <circle cx="11" cy="11" r="6"/><path d="M20 20l-4.5-4.5"/>
track       <path d="M4 12a8 8 0 0114-5.3M20 4v4h-4"/><path d="M20 12a8 8 0 01-14 5.3M4 20v-4h4"/>
database    <ellipse cx="12" cy="5" rx="7" ry="2.6"/><path d="M5 5v6c0 1.5 3.1 2.6 7 2.6s7-1.1 7-2.6V5M5 11v6c0 1.5 3.1 2.6 7 2.6s7-1.1 7-2.6v-6"/>
experts     <circle cx="9" cy="8" r="3"/><path d="M3.5 20a5.5 5.5 0 0111 0"/><circle cx="17" cy="9" r="2.2"/><path d="M15.5 20a4.5 4.5 0 016.5-4"/>
handshake   <path d="M12 8l2-2a2 2 0 013 0l3 3-4 4-2-2"/><path d="M12 8l-2-2a2 2 0 00-3 0L4 9l4 4 2-2"/><path d="M8 13l2 2M11 16l2 2"/>
alert       <path d="M12 3l9 16H3z"/><path d="M12 10v4M12 16.5h.02"/>
funnel/balance/gauge 用於「篩選／雙門檻／評分」三件事，是本專案的固定語彙
```

填色型（`fill`，無描邊）：

```
jet  <path d="M12 1.6c.7 0 1.15.8 1.3 2l.45 3.3 7.25 4.2v1.7l-6.95-2 .35 3.7 2.15 2v1.5l-3.2-1.1-.55 1.1-.55-1.1-3.2 1.1v-1.5l2.15-2 .35-3.7-6.95 2v-1.7l7.25-4.2.45-3.3c.15-1.2.6-2 1.3-2z"/>
```

### 6.3 主題對應（本專案固定用法）

| 主題 | 圖示 |
|---|---|
| 武器裝備／交貨 | `jet`（戰機）、`tank`（戰車）、`box`（交運驗收） |
| 金流 | `payment` 付款、`bank` FRB 帳戶、`withdraw` 提領 |
| 分析方法 | `funnel` 篩選、`balance` 雙門檻、`gauge` 風險評分、`scatter` 氣泡圖 |
| 資訊科技 | `chip` AI／演算、`memcpu` 本機運算、`doc` 報表、`database` 資料來源 |
| 機敏安全 | `shieldcheck`、`lock`、`wifioff` |
| 審計作為 | `search` 查核、`track` 跨期追蹤、`alert` 風險示警、`experts` 外部專家、`handshake` 跨單位協力 |

**武器圖示原則**：用戰機／戰車，**不要**飛彈、火箭；圖示下方**不加小標籤**；顏色與同頁其他圖示一致，不特別跳色。

### 6.4 其他圖像

- **LLM 品牌 logo（OpenAI／Claude／Gemini）可以出現在正式簡報**，用於標示「以大型語言模型協助整理」。
- 允許使用真實照片、機關 logo、系統截圖——使用者偏好「具體證據」勝過抽象示意。
- **不用卡通人物插圖。**

---

## 7. 文案慣例

1. **一句一行**。不要用逗號把三件事串成一長段；每句話結尾句號，各自獨立成行。
2. **先講白話再講術語**。技術項目一律配一句「這是什麼／為什麼重要」。
3. **口語過渡語可以留**（「比對剛剛篩選出的軍購案」「有瀏覽器就行」），因為是要唸出來的。
4. **關鍵詞用紅色粗體**，每頁 **3~5 個詞為上限**，只標名詞或判準（如「先付款後交貨」「雙門檻」「在途物資」）。
5. **舉例要寫具體數字**（如「結匯率 100％得 1.00 分，達 300％以上得 10.00 分」），不要只寫「依嚴重程度計分」。
6. 用語統一：
   - 「策略」不用「方法」
   - 「LLM」不用「AI」（指工具時）
   - 「評分項目」不用「維度」
   - 系統全稱：**國軍軍購案數據審核分析系統（DBA）**
7. 抽象的「過去 vs 現在」對比頁、純概念頁**不受青睞**；能換成真表格、真截圖、真規則就換掉。

---

## 8. 產出技術規範（pptxgenjs）

```js
const pptx = new PptxGenJS();
pptx.layout = 'LAYOUT_WIDE';          // 必須在 addSlide 之前設定
const IN = px => px / 96;             // 樣稿 px → 英吋
const PT = px => +(px * 0.75);        // 樣稿 px → pt
```

陷阱清單（踩過的）：

- 色碼**不加 `#`**、不可用 8 位含 alpha（會壞檔）；透明用 `transparency: 0-100`。
- **不要共用 options 物件**（pptxgenjs 會就地改寫成 EMU）。
- 陰影 `offset` 不可為負；要向上投影用 `angle: 270`。
- 字距是 `charSpacing`，不是 `letterSpacing`。
- 文字方塊有內距，要與線／形對齊時設 `margin: 0`。
- `rectRadius` 只對 `ROUNDED_RECTANGLE` 有效。
- 圖片用 `addImage({ data: 'image/png;base64,' + buf.toString('base64') })`，前綴不可省。
- 每個輸出檔用一個新的 `new PptxGenJS()`。
- 產出後跑 `python .../pptx/scripts/office/validate.py out.pptx`。

**視覺 QA 流程**（本環境 LibreOffice 無法渲染 Office 檔）：
寫一份 1280×720 的 HTML 複刻該頁 → Chromium/Playwright 截圖 → 目視檢查 → 座標 1:1 搬進 pptxgenjs。
`px → in = /96`、`px → pt = ×0.75`，兩邊完全對得上。

---

## 9. 交付前檢查表

- [ ] 每頁字級是否符合第 3 節（內文 ≥16 pt）
- [ ] 標題是否單層、策略頁是否為「策略N：」
- [ ] **全份沒有頁碼、沒有頁尾**（使用者自行以母片設定）
- [ ] 每頁紅色關鍵詞 ≤5 處；強調色只有琥珀
- [ ] 沒有 Office 預設橘／藍
- [ ] 分隔頁乾淨、且**有主題圖示**
- [ ] 表格有「意涵」欄；一頁 ≤11 列
- [ ] 截圖頁只有說明框、沒有標題
- [ ] 沒有物件超出版面（除階梯卡片刻意出血）、沒有殘留在角落的舊物件
- [ ] 文字沒有溢出色塊、沒有蓋到隔壁欄
- [ ] 圖示為獨立物件、未與文字群組
- [ ] 需要使用者自行補的圖／截圖已列清單

---

## 10. 禁止事項

- 標題下方加裝飾色線、色條、側邊色帶（AI 味）
- 內文置中（只有標題可以置中）
- 純文字頁（每頁至少一個圖示、圖表、表格或截圖）
- 卡通人物插圖
- 為了塞下內容而把字縮到 14 pt 以下
- 用不同顏色標示武器圖示、或在武器圖示下加小標籤
- 兩層標題（小標籤＋主標）
- 「壹貳參肆」中文章節序號
- 自行加上頁碼或頁尾（一律留給使用者於母片統一設定）

---

## 11. 尚待使用者確認（下次順手決定即可）

1. 關鍵詞紅：本檔採 `#C00000`（深紅，投影較穩）；使用者原習慣為純紅 `#FF0000`。要改回請說一聲。
2. 分隔頁四角迷彩淡影：素材來自 Slidesgo 軍事模板。無素材時是否可省略，或要另外準備一組？
3. 30 分鐘版（35 頁）是否也要一併套用本規範重製。
4. 表格自訂樣式（深藍表頭／隔行淡灰）尚未實作過，第一次產出後請確認觀感。

---

## 12. 版本記錄

| 日期 | 內容 |
|---|---|
| 115.07.26 | 首版。依 `簡報_15分鐘版.pptx`（使用者改版）與 `【第二廳】行政法人審計事務` 簡報彙整而成 |
| 115.07.27 | 修正第 24 項：改為**整份簡報都不放頁碼、不放頁尾**，由使用者自行於母片設定 |
